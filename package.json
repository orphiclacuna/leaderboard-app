{
  "name": "leaderboard",
  "version": "1.0.0",
  "description": "Leaderboard application with React frontend and Node.js backend",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "react-router-dom": "^7.7.0"
  }
}
